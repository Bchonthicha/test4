import { NavRouteTopicPipe } from './nav-route-topic.pipe';

describe('NavRouteTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new NavRouteTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
